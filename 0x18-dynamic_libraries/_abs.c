#include "main.h"

/**
 * _abs - Absolute valuu of a number
 * @r :An input
 * Description: Computing the absolute value of an input
 * Return: Absolute value of an input
 */

int _abs(int r)
{

	if (r >= 0)
		return (r);

	else
		return (r * -1);


}
